import React from 'react';

import { Typography, Box } from '@mui/material';

const Test: React.FC = () => {
    return (
        <Box>
            <Typography variant='body1'>Hello World</Typography>
            <Typography variant='body1'>Bye</Typography>
        </Box>
    );
};

export default Test;
